# georgedong32-pi-review

Pin: `npm:@georgedong32/pi-review` in source `settings.json`. Upstream: <https://github.com/GeorgeDong32/pi-review> Landing: [SPEC.md](./SPEC.md). Machine: [sidecars.json](./sidecars.json). Install: [guides/install-packages.md](../../guides/install-packages.md).

SoT sidecar is `.pi/agent/pi-review.json`, landed as a hardlink because saves `rename` a temp file onto the path. Source pins xAI grok-4.6 at thinking `low` for the gate and every bundled reviewer. Command is `/review`; it does not run by itself.
